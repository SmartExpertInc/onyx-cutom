# 🎯 Quality Tier Fix Instructions

## Проблема
В таблице "Block 2. Production Hours by Quality Level" отображаются данные только для "Level 2 - Interactive", а остальные уровни качества остаются пустыми, несмотря на то, что соответствующие проекты существуют в базе данных.

## ✅ Что уже исправлено

### Backend (onyx-cutom/custom_extensions/backend/main.py)
1. **Добавлено поле `quality_tier` в `ProjectApiResponse`** - теперь endpoint `/api/custom/projects` возвращает quality_tier данные
2. **Добавлено логирование** для отслеживания quality_tier данных в endpoint
3. **Исправлена логика расчета** `total_creation_hours` с учетом quality_tier

### Frontend (onyx-cutom/custom_extensions/frontend/src/components/ProjectsTable.tsx)
1. **Добавлено подробное логирование** для отслеживания данных в Block 2
2. **Исправлена логика маппинга** старых и новых названий quality tier
3. **Добавлены поля** `project_name` и `microproduct_name` в интерфейс `BackendProject`

## 🔍 Диагностика проблемы

### Шаг 1: Проверьте логи backend
```bash
# Запустите backend
cd onyx-cutom
docker-compose -f deployment/docker_compose/docker-compose.dev.yml up backend -d

# Проверьте логи
docker-compose -f deployment/docker_compose/docker-compose.dev.yml logs backend --tail=100
```

Ищите в логах:
- `[PDF_ANALYTICS] /api/custom/projects: Project X: quality_tier=Y`
- `[PDF_ANALYTICS] Quality tier distribution being sent to frontend: {...}`

### Шаг 2: Проверьте API endpoint
```bash
# Запустите тест API endpoint
python test_api_endpoint.py
```

Этот скрипт покажет:
- Какие quality_tier данные приходят с backend
- Распределение проектов по уровням качества
- Результат симуляции frontend расчета

### Шаг 3: Проверьте frontend логи
1. **Пересоберите frontend**:
```bash
cd onyx-cutom/custom_extensions/frontend
npm run build
```

2. **Запустите frontend**:
```bash
cd onyx-cutom
docker-compose -f deployment/docker_compose/docker-compose.dev.yml up custom_frontend -d
```

3. **Откройте браузер** и перейдите на страницу проектов
4. **Откройте Developer Tools** (F12) → Console
5. **Откройте превью проектов** и ищите логи:
   - `[FRONTEND_DEBUG] === BLOCK 2 QUALITY TIER SUMS START ===`
   - `[FRONTEND_DEBUG] === DETAILED PROJECT ANALYSIS ===`
   - `[FRONTEND_DEBUG] === QUALITY TIER PROCESSING ===`

## 🛠️ Возможные решения

### Если backend не отправляет quality_tier данные:
1. Проверьте, что в базе данных у проектов есть поле `quality_tier`
2. Убедитесь, что backend правильно читает это поле из базы данных
3. Проверьте, что в модели `ProjectApiResponse` есть поле `quality_tier`

### Если frontend не получает данные:
1. Проверьте, что API endpoint возвращает правильные данные
2. Убедитесь, что frontend правильно обрабатывает ответ от API
3. Проверьте логи в консоли браузера

### Если данные приходят, но не отображаются:
1. Проверьте логику маппинга quality tier в frontend
2. Убедитесь, что расчет сумм работает правильно
3. Проверьте, что все quality tier имеют данные

## 📊 Ожидаемый результат

После исправления в Block 2 должны отображаться данные для всех уровней качества:
- **Level 1 - Basic**: данные от проектов с `quality_tier = 'basic'` или `'starter'`
- **Level 2 - Interactive**: данные от проектов с `quality_tier = 'interactive'`, `'medium'` или `null`
- **Level 3 - Advanced**: данные от проектов с `quality_tier = 'advanced'`
- **Level 4 - Immersive**: данные от проектов с `quality_tier = 'immersive'` или `'professional'`

## 🔧 Дополнительные тесты

### Тест frontend логики:
```bash
node test_frontend_quality_tiers.js
```

### Тест backend логики:
```bash
python test_backend_quality_tiers.py
```

## 📝 Логи для анализа

Пожалуйста, предоставьте следующие логи для дальнейшей диагностики:

1. **Backend логи** (последние 50-100 строк)
2. **Результат выполнения** `python test_api_endpoint.py`
3. **Frontend логи из консоли браузера** (при открытии превью проектов)

Это поможет точно определить, на каком этапе теряются данные quality tier. 