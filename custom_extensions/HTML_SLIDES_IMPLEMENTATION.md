# HTML Slides Implementation - Полное соответствие React компонентам

## 📋 Обзор

Все 6 запрошенных слайдов успешно добавлены в `avatar_slide_template.html` с полным соответствием React компонентам.

## ✅ Реализованные слайды

### 1. **CourseOverview** (`course-overview-slide`)
**Файл React:** `CourseOverviewSlideTemplate.tsx`

**Структура:**
- Левая панель (45%): градиент синий, логотип вверху, заголовок+подзаголовок по центру, номер страницы внизу
- Правая панель (55%): белый фон с изображением

**Ключевые стили:**
- Градиент: `linear-gradient(90deg, #0F58F9 0%, #1023A1 100%)`
- Размер заголовка: 95px
- Изображение: height 91%, bottom -40px

**Переменные:**
- `title` - заголовок
- `subtitle` - подзаголовок
- `imagePath` - путь к изображению
- `logoPath` - путь к логотипу
- `pageNumber` - номер страницы

---

### 2. **WorkLifeBalance** (`work-life-balance-slide`)
**Файл React:** `WorkLifeBalanceSlideTemplate.tsx`

**Структура:**
- Левая область (60%): градиент розовый-фиолетовый, логотип, заголовок, контент, номер страницы
- Правая область (40%): белая арка (rotate 90deg), изображение

**Ключевые стили:**
- Градиент: `linear-gradient(90deg, #DE57E5 0%, #8863FB 100%)`
- Размер заголовка: 87px
- Арка: width 116%, height 74%, border-radius 50%

**Переменные:**
- `title` - заголовок
- `content` - текст контента
- `imagePath` - путь к изображению
- `logoPath` - логотип
- `pageNumber` - номер страницы

---

### 3. **PhishingDefinition** (`phishing-definition-slide`)
**Файл React:** `PhishingDefinitionSlideTemplate.tsx`

**Структура:**
- Левая секция (50%): светло-синий фон, заголовок, список определений, круглое фото внизу, номер страницы
- Правая секция (50%): полное изображение, логотип внизу справа

**Ключевые стили:**
- Фон левой части: #E0E7FF
- Заголовок: 75px, color #212222
- Определения: 21px, color #545555
- Профильное фото: 240px круг

**Переменные:**
- `title` - заголовок
- `definitions` - массив определений
- `profileImagePath` - профильное фото
- `rightImagePath` - правое изображение
- `logoPath` - логотип
- `pageNumber` - номер страницы

---

### 4. **BenefitsList** (`benefits-list-slide`)
**Файл React:** `BenefitsListSlideTemplate.tsx`

**Структура:**
- Верхняя секция (640px): градиент синий, badge с subtitle, заголовок, описание, навигационные квадраты, профильное фото
- Нижняя секция (flex 1): светло-синий фон, сетка 3 колонки с льготами

**Ключевые стили:**
- Градиент верха: `linear-gradient(180deg, #0F58F9 0%, #1023A1 100%)`
- Заголовок: 72px, Lora serif
- Описание: 42px
- Льготы: grid 3 колонки, 36px

**Переменные:**
- `title` - заголовок
- `subtitle` - подзаголовок в badge
- `description` - описание
- `benefits` - массив льгот
- `currentStep` - текущий шаг навигации
- `totalSteps` - всего шагов
- `profileImagePath` - профильное фото
- `logoNew` - логотип
- `pageNumber` - номер страницы

---

### 5. **ImpactStatements** (`impact-statements-slide`)
**Файл React:** `ImpactStatementsSlideTemplate.tsx`

**Структура:**
- Левая секция (45%): заголовок + синий градиентный блок с профильным фото
- Правая секция (55%): 3 карточки статистики (2 слева, 1 справа)

**Ключевые стили:**
- Фон: #E0E7FF
- Заголовок: 69px, Lora serif
- Градиентный блок: 735x465px, синий градиент
- Карточки статистики: белые с тенью
- Цифры в карточках: 87px, Lora serif

**Переменные:**
- `title` - заголовок
- `statements` - массив объектов {number, description}
- `profileImagePath` - профильное фото
- `logoNew` - логотип
- `pageNumber` - номер страницы

---

### 6. **HybridWorkBestPractices** (`hybrid-work-best-practices-slide`)
**Файл React:** `HybridWorkBestPracticesSlideTemplate.tsx`

**Структура:**
- Логотип сверху слева
- Левая колонка (50%): тег с точкой, главное утверждение, профильное фото внизу
- Правая колонка (55%): сетка 2x2 с практиками, изображение команды внизу

**Ключевые стили:**
- Фон: #E0E7FF
- Тег: border 3px solid black, border-radius 75px
- Main statement: 53px, Lora serif, font-weight 600
- Практики: grid 2x2, синие квадратики с номерами
- Изображение команды: height 46%

**Переменные:**
- `title` - заголовок (в теге)
- `mainStatement` - главное утверждение
- `practices` - массив объектов {number, title, description}
- `profileImagePath` - профильное фото
- `teamImagePath` - изображение команды
- `logoPath` - логотип в хедере
- `logoNew` - логотип внизу
- `pageNumber` - номер страницы

---

## 🎨 Шрифты

Все слайды используют правильные шрифты согласно настройкам:
- **Inter** - для всех UI элементов (контент, тексты, описания)
- **Lora** - для заголовков в слайдах (класс `.title-element`)

## 📐 Размеры

Все стили масштабированы для видео размера **1920x1080px** (Full HD):
- HTML body: `width: 1920px; height: 1080px;`
- Все размеры шрифтов, паддинги, margins пересчитаны из React (1200px canvas) в видео размер

## 🔄 Масштабирование

В HTML шаблоне используется система масштабирования:
```jinja2
{% set EDITOR_WIDTH = metadata.canvasDimensions.width or 1174 %}
{% set EDITOR_HEIGHT = metadata.canvasDimensions.height or 600 %}
{% set VIDEO_WIDTH = 1920 %}
{% set VIDEO_HEIGHT = 1080 %}
{% set SCALE_X = VIDEO_WIDTH / EDITOR_WIDTH %}
{% set SCALE_Y = VIDEO_HEIGHT / EDITOR_HEIGHT %}
```

## 🎯 Использование

В Python/Flask рендере передайте:
```python
render_template('avatar_slide_template.html',
    templateId='course-overview-slide',  # или другой ID
    title='Course',
    subtitle='Overview',
    imagePath='/path/to/image.jpg',
    logoPath='/path/to/logo.png',
    pageNumber='01',
    theme='dark-purple'
)
```

## 📝 Файл

Все HTML слайды находятся в:
**`custom_extensions/backend/templates/avatar_slide_template.html`**

## ✨ Что дальше?

Слайды готовы к использованию в системе генерации видео! Каждый слайд:
- ✅ Точно соответствует React версии по верстке
- ✅ Использует правильные шрифты (Inter + Lora)
- ✅ Масштабирован для 1920x1080
- ✅ Поддерживает все необходимые переменные
- ✅ Готов к рендерингу в видео
