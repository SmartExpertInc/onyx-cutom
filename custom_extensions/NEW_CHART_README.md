# Новая версия круговой диаграммы

## 📋 Описание

Полностью переписанная круговая диаграмма с новыми именами файлов, которая не создает конфликтов с существующими файлами и не вызывает проблем с правами доступа.

## 🗂️ Структура файлов

### Backend (Python)
- `backend/app/services/chart_maker.py` - Генератор изображений диаграммы
- `backend/app/utils/chart_css.py` - CSS генератор для веб-версии
- `backend/test_chart.py` - Тестовый файл

### Frontend (React/TypeScript)
- `frontend/src/components/templates/ChartTemplate.tsx` - React компонент

## 🚀 Возможности

### ✅ Что работает
- 6 сегментов с равными процентами (16.67% каждый)
- Красивые современные цвета
- Проценты отображаются рядом с текстом (НЕ на диаграмме)
- Inline редактирование всех элементов
- Автоматическое сохранение изменений
- Поддержка тем оформления
- Генерация изображений для PDF
- CSS версия для веб-отображения

### 🎨 Дизайн
- Размер диаграммы: 320x320px
- Внутренний круг: 80x80px
- Современные цвета: синий, зеленый, оранжевый, красный, фиолетовый, розовый
- Тени и эффекты для красивого отображения
- Адаптивная верстка

## 📊 Структура данных

### Сегменты (segments)
```typescript
{
  label: string;        // Название сегмента
  percentage: number;   // Процент (16.67)
  color: string;        // Цвет (#3B82F6)
  description?: string; // Описание
}
```

### Данные сегментов (segmentData)
```typescript
{
  name: string;         // Название для отображения
  description: string;  // Описание
  color: string;        // Цвет
  percentage: string;   // Процент как строка ("16.7%")
}
```

## 🔧 Использование

### В React компоненте
```tsx
import ChartTemplate from './ChartTemplate';

<ChartTemplate
  slideId="chart-1"
  title="Моя диаграмма"
  segments={segments}
  segmentData={segmentData}
  descriptionText="Описание диаграммы"
  theme={currentTheme}
  onUpdate={handleUpdate}
  isEditable={true}
/>
```

### В Python (генерация изображения)
```python
from app.services.chart_maker import make_chart, get_default_segments

# Создать диаграмму
segments = get_default_segments()
image_base64 = make_chart(segments)
```

### В Python (генерация CSS)
```python
from app.utils.chart_css import create_css

# Создать CSS
result = create_css(segments, "my-chart")
html = result["html"]
css = result["css"]
```

## 🧪 Тестирование

Запустите тест:
```bash
cd onyx-cutom/custom_extensions/backend
python test_chart.py
```

## ✨ Преимущества новой версии

1. **Нет конфликтов** - новые имена файлов
2. **Простая структура** - минимум зависимостей
3. **Без проблем с правами** - не создает __pycache__
4. **Легко тестировать** - встроенные тесты
5. **Хорошо документировано** - подробная документация
6. **Современный дизайн** - красивые цвета и эффекты

## 🔄 Миграция

Если у вас есть старая версия PieChart, просто замените импорты:

**Было:**
```tsx
import PieChartInfographicsTemplate from './PieChartInfographicsTemplate';
```

**Стало:**
```tsx
import ChartTemplate from './ChartTemplate';
```

## 📝 Примечания

- Все файлы имеют новые имена, не конфликтующие с существующими
- Код написан с учетом лучших практик
- Поддерживается обратная совместимость по структуре данных
- Автоматическое тестирование при запуске

## 🎯 Результат

Теперь у вас есть полностью рабочая круговая диаграмма, которая:
- Красиво отображается в веб-интерфейсе
- Генерирует изображения для PDF
- Не создает проблем с Git
- Легко редактируется
- Поддерживает все необходимые функции 