# Простая версия круговой диаграммы

## Обзор

Создана альтернативная простая версия круговой диаграммы, которая работает без проблем с правами доступа и сложных зависимостей.

## Файлы

### 1. Генератор изображений
**Файл:** `backend/app/services/simple_pie_chart_generator.py`

- ✅ Простой код без сложных эффектов
- ✅ Минимальные зависимости (только PIL)
- ✅ Без кэша и временных файлов
- ✅ Прямая генерация PNG изображений

### 2. CSS генератор
**Файл:** `backend/app/utils/simple_css_generator.py`

- ✅ Простой conic-gradient без сложных вычислений
- ✅ Базовые стили без теней и эффектов
- ✅ Минимальный HTML/CSS код

### 3. Калькулятор
**Файл:** `backend/app/utils/simple_calculator.py`

- ✅ Простые математические вычисления
- ✅ Валидация данных
- ✅ Создание сегментов по умолчанию

### 4. Тест
**Файл:** `backend/test_simple_pie_chart.py`

- ✅ Полный тест всех компонентов
- ✅ Проверка генерации изображений
- ✅ Проверка CSS кода

## Использование

### Генерация изображения
```python
from app.services.simple_pie_chart_generator import simple_pie_generator

segments = [
    {"percentage": 16.67, "color": "#3B82F6"},
    {"percentage": 16.67, "color": "#10B981"},
    # ... еще сегменты
]

# Генерация base64
image_base64 = simple_pie_generator.generate_pie_chart(segments)

# Сохранение в файл
simple_pie_generator.generate_pie_chart(segments, "output.png")
```

### Генерация CSS
```python
from app.utils.simple_css_generator import generate_simple_css

result = generate_simple_css(segments, "my-chart")
html = result["html"]
css = result["css"]
```

### Валидация данных
```python
from app.utils.simple_calculator import validate_data, calculate_segments

# Валидация
validation = validate_data(segments)
if validation['valid']:
    print("Данные корректны")

# Расчет углов
angles = calculate_segments(segments)
```

## Преимущества простой версии

1. **Надежность**: Нет проблем с правами доступа
2. **Простота**: Минимальный код без сложных зависимостей
3. **Скорость**: Быстрая генерация без кэша
4. **Совместимость**: Работает на любом сервере
5. **Отладка**: Легко найти и исправить проблемы

## Тестирование

```bash
cd onyx-cutom/custom_extensions/backend
python test_simple_pie_chart.py
```

## Результат

Простая версия создает:
- ✅ Красивую круговую диаграмму 320x320px
- ✅ 6 сегментов с современными цветами
- ✅ Внутренний круг (donut hole)
- ✅ Белые границы между сегментами
- ✅ CSS код для веб-отображения

## Цветовая схема

- 🔵 Синий: `#3B82F6`
- 🟢 Зеленый: `#10B981`
- 🟡 Желтый: `#F59E0B`
- 🔴 Красный: `#EF4444`
- 🟣 Фиолетовый: `#8B5CF6`
- 🟠 Розовый: `#EC4899`

## Миграция

Для использования простой версии замените импорты:

```python
# Вместо сложной версии
from app.services.pie_chart_generator import pie_chart_generator
from app.utils.pie_chart_css_generator import generate_css_pie_chart

# Используйте простую версию
from app.services.simple_pie_chart_generator import simple_pie_generator
from app.utils.simple_css_generator import generate_simple_css
```

Эта версия решает все проблемы с правами доступа и работает стабильно на любом сервере! 