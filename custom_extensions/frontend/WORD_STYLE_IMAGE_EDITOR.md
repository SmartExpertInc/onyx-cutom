# Microsoft Word-Style Image Editor

Цей компонент надає інтерфейс редагування зображень в стилі Microsoft Word з інтуїтивними інструментами та знайомими патернами взаємодії.

## 🎯 Основні можливості

### 1. **Word-Style Toolbar (Панель інструментів)**
- З'являється при кліку на зображення
- Розташована під зображенням
- Містить всі основні інструменти редагування

### 2. **Context Menu (Контекстне меню)**
- Викликається правим кліком миші
- Швидкий доступ до основних функцій
- Знайомий інтерфейс як у Word

### 3. **Top Toolbar (Верхня панель)**
- Фіксована панель зверху сторінки
- Кнопка "Insert Picture" для додавання зображень
- Інші інструменти форматування

## 🖱️ Способи взаємодії

### **Лівий клік на зображенні**
- Відкриває панель інструментів
- Показує всі доступні опції редагування
- Панель з'являється під зображенням

### **Правий клік на зображенні**
- Відкриває контекстне меню
- Швидкий доступ до основних функцій
- Меню з'являється біля курсора

### **Клік поза меню**
- Автоматично закриває всі відкриті меню
- Повертає до звичайного режиму перегляду

## 🛠️ Доступні інструменти

### **Size Controls (Керування розміром)**
- **Smaller** - зменшує зображення на 25px
- **Larger** - збільшує зображення на 25px
- **Reset** - повертає до стандартного розміру (300px)
- **Current Width** - показує поточну ширину

### **Layout Controls (Керування макетом)**
- **In Line** - зображення в рядку з текстом
- **Square** - квадратне обтікання зліва
- **Tight** - тісне обтікання справа
- **Behind** - зображення позаду тексту

### **Alignment Controls (Вирівнювання)**
- **Left** - вирівнювання по лівому краю
- **Center** - вирівнювання по центру
- **Right** - вирівнювання по правому краю

### **Border Controls (Рамки)**
- **None** - без рамки
- **Square** - квадратні кути
- **Rounded** - закруглені кути

## 🎨 Візуальні індикатори

### **Selection Indicator**
- Сині рамка навколо зображення при наведенні
- Показує, що зображення вибрано для редагування
- З'являється тільки в режимі редагування

### **Hover Effects**
- Тінь збільшується при наведенні
- Курсор змінюється на pointer
- Плавні анімації переходів

## 📁 Структура файлів

```
custom_extensions/frontend/src/components/
├── TextPresentationDisplay.tsx     # Основний компонент з Word-стиль інтерфейсом
└── WordStyleImageEditor.tsx        # Окремий компонент для тестування
```

## 🔧 Технічна реалізація

### **Компоненти**
1. **WordStyleImageToolbar** - панель інструментів
2. **WordStyleContextMenu** - контекстне меню
3. **WordStyleTopToolbar** - верхня панель
4. **WordStyleImageEditor** - окремий компонент для тестування

### **Стан компонентів**
```typescript
const [showWordToolbar, setShowWordToolbar] = useState(false);
const [showContextMenu, setShowContextMenu] = useState(false);
const [toolbarPosition, setToolbarPosition] = useState({ x: 0, y: 0 });
const [contextMenuPosition, setContextMenuPosition] = useState({ x: 0, y: 0 });
```

### **Обробники подій**
```typescript
const handleImageClick = (event: React.MouseEvent) => {
  // Відкриває панель інструментів
};

const handleImageRightClick = (event: React.MouseEvent) => {
  // Відкриває контекстне меню
};

const handleImageChange = (updates: Partial<ImageBlock>) => {
  // Застосовує зміни до зображення
};
```

## 🚀 Використання

### **В основному компоненті**
```typescript
import TextPresentationDisplay from './TextPresentationDisplay';

<TextPresentationDisplay
  dataToDisplay={data}
  isEditing={true}
  onTextChange={handleTextChange}
/>
```

### **Окремий компонент**
```typescript
import WordStyleImageEditor from './WordStyleImageEditor';

<WordStyleImageEditor
  imageBlock={imageBlock}
  onImageChange={handleImageChange}
  isEditing={true}
/>
```

## 🎯 Переваги Word-стиль інтерфейсу

1. **Знайомий UX** - користувачі знають як працювати з Word
2. **Інтуїтивність** - логічні патерни взаємодії
3. **Швидкість** - контекстне меню для швидких дій
4. **Повнота** - панель інструментів для детальних налаштувань
5. **Візуальна зворотній зв'язок** - індикатори вибору та стану

## 🔄 Оновлення та розширення

### **Додавання нових інструментів**
1. Додайте нову секцію в `WordStyleImageToolbar`
2. Створіть відповідну функцію в `handleImageChange`
3. Оновіть типи `ImageBlock` якщо потрібно

### **Кастомізація стилів**
- Всі стилі використовують Tailwind CSS
- Легко змінювати кольори, розміри, анімації
- Підтримка темної теми через CSS змінні

### **Додавання нових режимів макету**
1. Розширте тип `layoutMode` в `ImageBlock`
2. Додайте відповідну логіку рендерингу
3. Оновіть інтерфейс вибору

## 🐛 Відомі обмеження

1. **Z-index** - меню використовують високі z-index значення
2. **Positioning** - позиціонування може потребувати налаштування на різних екранах
3. **Touch devices** - контекстне меню може не працювати на мобільних пристроях

## 🔮 Майбутні покращення

1. **Touch support** - підтримка сенсорних екранів
2. **Keyboard shortcuts** - гарячі клавіші
3. **Undo/Redo** - історія змін
4. **Drag & Drop** - перетягування для зміни розміру
5. **Image filters** - фільтри та ефекти
6. **Crop tool** - обрізання зображень
