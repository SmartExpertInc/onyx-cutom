# Функціонал обтікання фото в ImageBasicActions

## Опис

Додано повний функціонал для обтікання фото (розміщення тексту зліва/справа від фото) в dropdown меню `ImageBasicActions`. Тепер користувачі можуть легко налаштовувати layout зображень без необхідності відкривати повний редактор.

## 🎯 **Нові можливості**

### **Layout Options (Режими макету)**

#### 1. **Standalone** 🖼️
- Зображення на окремому рядку
- Повна ширина контейнера
- Текст не обтікає зображення

#### 2. **Inline Left** 🖼️ 📄
- Зображення зліва
- Текст обтікає зображення справа
- Ідеально для портретних зображень

#### 3. **Inline Right** 📄 🖼️
- Зображення справа
- Текст обтікає зображення зліва
- Ідеально для ландшафтних зображень

#### 4. **Side-by-side Left** 🖼️ | 📄
- Зображення зліва, текст справа
- Рівна висота елементів
- Контроль пропорцій

#### 5. **Side-by-side Right** 📄 | 🖼️
- Текст зліва, зображення справа
- Рівна висота елементів
- Контроль пропорцій

### **Space Distribution (Розподіл простору)**

Для side-by-side режимів доступні пропорції:

- **50% - 50%** - Рівні частки
- **60% - 40%** - Зображення більше
- **40% - 60%** - Контент більше
- **70% - 30%** - Зображення домінує
- **30% - 70%** - Контент домінує

## 🖥️ **Інтерфейс користувача**

### **Dropdown структура:**

```
📋 Quick Actions
├── 🎨 Layout Options
│   ├── 🖼️ Standalone
│   ├── 🖼️ 📄 Inline Left
│   ├── 📄 🖼️ Inline Right
│   ├── 🖼️ | 📄 Side-by-side Left
│   └── 📄 | 🖼️ Side-by-side Right
├── 📊 Space Distribution (тільки для side-by-side)
│   ├── 50% - 50%
│   ├── 60% - 40%
│   ├── 40% - 60%
│   ├── 70% - 30%
│   └── 30% - 70%
├── 📏 Size
├── ↔️ Alignment
├── 🎨 Corner Rounding
└── 👁️ Layout Preview
```

### **Візуальні індикатори:**

- **🖼️** - Зображення
- **📄** - Текст/контент
- **|** - Розділювач для side-by-side
- **🖼️ 📄** - Inline layout
- **🖼️ | 📄** - Side-by-side layout

## 🔧 **Технічна реалізація**

### **Структура даних:**

```typescript
interface ImageBlock {
  layoutMode?: 'standalone' | 'side-by-side-left' | 'side-by-side-right' | 'inline-left' | 'inline-right';
  layoutProportion?: '50-50' | '60-40' | '40-60' | '70-30' | '30-70';
  // ... інші властивості
}
```

### **CSS класи для різних режимів:**

```css
/* Inline режими */
.inline-left {
  float: left;
  max-width: 50%;
  margin: 16px 16px 16px 0;
}

.inline-right {
  float: right;
  max-width: 50%;
  margin: 16px 0 16px 16px;
}

/* Side-by-side режими */
.side-by-side-left {
  display: flex;
  align-items: flex-start;
}

.side-by-side-right {
  display: flex;
  flex-direction: row-reverse;
  align-items: flex-start;
}
```

## 📱 **Користування**

### **Швидке налаштування:**

1. **Клікніть на зображення** в режимі редагування
2. **Натисніть кнопку ⚙️** (Settings) біля зображення
3. **Виберіть Layout Options** у dropdown
4. **Оберіть потрібний режим** з візуальними превью
5. **Налаштуйте пропорції** (для side-by-side режимів)

### **Приклади використання:**

#### **Для портретних зображень:**
- **Inline Left** - зображення зліва, текст справа
- **Side-by-side Left** - зображення зліва, текст справа (рівна висота)

#### **Для ландшафтних зображень:**
- **Inline Right** - зображення справа, текст зліва
- **Side-by-side Right** - текст зліва, зображення справа

#### **Для великих зображень:**
- **Standalone** - зображення на окремому рядку
- **Side-by-side** з пропорцією 70-30 - зображення домінує

## 🎨 **Візуальний попередній перегляд**

Dropdown показує:
- **Превью режиму** з емодзі
- **Опис функціоналу** для кожного режиму
- **Поточний стан** у Layout Preview секції
- **Пропорції** для side-by-side режимів

## ✅ **Переваги**

1. **Швидкий доступ** - всі layout опції в одному dropdown
2. **Візуальна ясність** - емодзі та превью для кожного режиму
3. **Інтуїтивність** - зрозумілі назви та описи
4. **Гнучкість** - 5 різних режимів + пропорції
5. **Реальний час** - миттєве застосування змін

## 🔄 **Інтеграція**

Функціонал повністю інтегрований з:
- `TextPresentationDisplay.tsx` - основний рендеринг
- `WordStyleImageEditor.tsx` - повний редактор
- `textPresentation.ts` - типи даних
- PDF експорт - підтримка в шаблонах

## 📋 **Підтримувані режими**

| Режим | Опис | Використання |
|-------|------|-------------|
| `standalone` | Зображення на окремому рядку | Великі зображення, заголовки |
| `inline-left` | Зображення зліва, текст обтікає | Портретні зображення |
| `inline-right` | Зображення справа, текст обтікає | Ландшафтні зображення |
| `side-by-side-left` | Зображення зліва, текст справа | Порівняння, опис |
| `side-by-side-right` | Текст зліва, зображення справа | Інструкції, процеси |

Цей функціонал робить роботу з layout зображень максимально зручною та інтуїтивною для користувачів!
